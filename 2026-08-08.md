# 2026-08-08

- Built "Jogi IoT Search Engine" for user: single-file index.html, NVIDIA NIM backend with rotation across up to 10 free API keys (round-robin, 429 → 10-min cooldown, 401/403 → dead), keys in localStorage, GitHub Pages deploy. Files in projects/jogi-iot-search/.
- User hit "Failed to fetch" → verified via curl that integrate.api.nvidia.com sends NO access-control-allow-origin for public origins (only build.nvidia.com). Browser calls impossible → added Proxy URL setting + wrote worker.js (Cloudflare Worker CORS proxy, free 100k req/day).
- User pasted the new error back = hasn't deployed the worker yet. Gave step-by-step Cloudflare setup. Republished updated index.html + worker.js.
